# 2026-07-24 工作记录

## 悬浮窗改固定大小 + 鼠标交互（剪贴板QuickClipboard）

将预览"悬浮窗"从全屏穿透覆盖层改为**固定大小、可交互、常驻可钉住**的独立窗口。

### 用户确认的设计决策
- 预览窗口尺寸：设为**可调项**，默认 640×480，暴露于设置 UI（范围 240–1920 / 200–1200）。
- 显示/关闭行为：**常驻可钉住** —— 离开列表项不再自动关闭；用户可用按钮/Esc 关闭，或钉住。
- 滚轮触发：**直接滚轮** —— 悬停预览窗 + 滚轮直接缩放图片/滚动文本，无需 Ctrl。

### 改动文件
- `src-tauri/src/windows/preview_window/mod.rs`：固定尺寸常量、clamp、resolve_preview_window_rect、PREVIEW_PINNED 原子、set_preview_pinned 命令、create/show 去掉 ignore_cursor_events、focusable(true)。
- `src-tauri/src/lib.rs`：注册 set_preview_pinned。
- `src-tauri/src/services/system/raw_input.rs`：去掉离开主窗自动关预览；点击落在预览窗内则吞掉（不关主窗/预览）。
- `src/shared/services/settingsService.js`：defaultSettings 加 previewWindowWidth/Height。
- `src/windows/settings/sections/ClipboardSection.jsx`：加尺寸设置项（clampInt）。
- `src/shared/locales/zh-CN.json` / `en-US.json`：预览提示文案 + close/pin/pinned。
- `src/shared/api/previewWindow.js`：showPreviewWindow 读设置传尺寸；加 setPreviewPinned。
- `src/windows/main/components/ClipboardItem.jsx`：hover 行为改为常驻（不关预览）。
- `src/windows/main/components/FavoriteItem.jsx`：对齐 ClipboardItem 的常驻行为（scheduleHoverPreview 清计时器、handleMouseLeave 只清计时器、去掉操作组 onMouseEnter 关预览）。
- `src/windows/preview/App.jsx`：重写为固定窗交互（toolbar + 钉住/关闭、图片滚轮缩放+拖拽平移、文本/HTML/文件原生滚动、Esc 关闭、钉住时忽略数据更新）。

### 验证
- `vite build` 成功，无错误（仅已有 chunk 体积警告）。
- `cargo check` 成功，仅已有 dead-code 警告，无编译错误。

### 备注
- 预览钉住状态同时存于 Rust 原子（供 guard/show 逻辑判断）与 React state。
- 注意：FavoriteItem 仍有 handlePreviewWheel（Ctrl+滚轮，列表项上的旧交互），本次未改动，与"直接滚轮"的预览窗内交互不冲突。

## 修复"悬浮窗不显示"（13:4x 重新编译启动后）

### 根因
原显示链路是单点故障：Rust 用 `.visible(false)` 创建窗口，唯一 `.show()` 来自 React 收到数据后回传 `reveal_preview_window`；且卡片 `previewEntranceStyle` 初始 `opacity:0`，要等 reveal 成功才 `setIsVisible(true)`。只要 React 没收到事件 / `previewReady` 没变 true / request_id 不匹配，窗口就永远隐藏 + 卡片透明，表现为"不显示"。

### 修复
- `mod.rs`：`show_preview_window` 的**复用路径**与**新建路径**在 emit 数据后都直接 `window.show()`（Rust 端负责显示，不再依赖前端往返）。
- `App.jsx`：`applyPreviewData`（及挂载时 `get_preview_window_data`）拿到数据即 `setIsVisible(true)`，卡片立即可见。
- 保留 `reveal_preview_window` 作为冗余 show（无害），保留 [preview-debug] 诊断日志待确认。
- 复查了两个守卫 `is_context_menu_visible()` / 主窗 `WindowState::Visible`（visibility.rs:144 置 Visible），确认非本次阻塞点。

### 验证
- 重新 `tauri dev` 编译通过（34.60s），`Running QuickClipboard.exe`，进程存活。
- 待用户在主窗列表项上悬停验证窗口是否出现。

## 真正的根因：预览 webview 模块级 TDZ 崩溃（14:5x 最终定位）

前面"复用窗口 show 成功"等日志只是证明 Rust 侧正确显示了窗口，但 webview 内容从不渲染。
加了 JS→Rust 错误桥（`debug_log` 命令 + preview/index.html 内联 error-catcher）后，捕获到真实报错：

```
[preview-js] [error] Uncaught ReferenceError: Cannot access 'defaultSettings' before initialization
    @ http://localhost:1421/shared/store/settingsStore.js:6
```

### 根因
循环依赖：`settingsStore.js` → `settingsService.js` → `@shared/api`(barrel) → `previewWindow.js` → `settingsStore.js`。
`settingsStore` 第 10 行 `proxy({ ...defaultSettings })` 在模块求值期访问了 `defaultSettings`，
但此时 `settingsService`（被其导入）还没执行到第 14 行 `export const defaultSettings` 的定义，
于是 `defaultSettings` 处于 TDZ（暂时性死区），整个预览 webview 在 import 阶段就抛错、React 无法挂载 → 卡片永不渲染 → 表现为"悬浮窗不显示"。
主窗口不崩是因为入口 import 顺序恰好让 `settingsService` 先完整初始化（与预览入口顺序不同，所以踩中 TDZ）。

### 修复（已实施）
- 新建 `src/shared/services/defaultSettings.js`：把 `defaultSettings` 对象整体搬入这个**零依赖**模块。
- `settingsService.js`：改为 `import { defaultSettings } from './defaultSettings'` 并 `export { defaultSettings }`（re-export，旧调用方无需改）。
- `settingsStore.js`：`defaultSettings` 直接从这个零依赖模块导入；`loadSettingsFromBackend/saveSettingsToBackend` 仍来自 `settingsService`。
- 这样切除了 TDZ 循环：`defaultSettings` 来自独立、先行完整初始化的模块；`previewWindow.js` 只在函数体内访问 `settingsStore`，不会在模块顶层触碰 TDZ。

### 当前状态（15:1x 已闭环）
- 用户悬停后确认看到蓝色测试卡片（方案 A）→ **TDZ 根因确凿、修复生效、React 正常挂载渲染**。
- 已清理全部临时诊断代码：恢复 `.preview-card` 正式主题样式（color-mix 边框 + textContainerBackgroundColor）；
  删除 `debug_log` Rust 命令及其注册、preview/index.html 内联错误桥、index.jsx/App.jsx 的 `[preview-js]` 里程碑日志、
  mod.rs 所有 `[preview-debug]` 日志。grep 确认 `preview-debug|preview-js|debug_log|__preview_log` 全仓已无残留。
- 新增 `src/shared/services/defaultSettings.js` 已纳入 git；其余改动一并提交。
- git 备份提交：`5df723e5`（main 分支，16 文件，+789/−1243）。`.workbuddy/` 故意未纳入。未 push（远程 origin 存在，用户如需可手动 push）。
